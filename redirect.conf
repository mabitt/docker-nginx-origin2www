server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;
    if ( $host ~ ^www\. ) {
        return 409;
    }
    if ($host ~* ^([0-9]+)\.([0-9]+)\.([0-9]+)\.([0-9]+)$ ) {
	return 406;
    }
    return 301 $scheme://www.$host$request_uri;
}
